# iima

IINA 1.3.5 replication implemented with Tauri. Project release version: `0.9.0` (build `90`).

Reference source: `参考/iina`, branch `release/1.3.5`, commit `45187444`.

The detailed artifact hashes below describe the last locally verified reference-identity package.
The `0.9.0` packages are produced afresh by GitHub Actions and are not represented by those older
hashes.

## Current Milestone

- The Tauri 2 macOS application, initial window, playback window, native menu, and native media/rendering boundaries are in place with IINA 1.3.5 assets.
- Playback uses one persistent `libmpv` client per player session, a native `NSOpenGLView` render surface, authoritative mpv events/properties, and an isolated headless client for metadata/track/chapter probing plus thumbnail extraction. Both paths use the packaged libmpv/FFmpeg runtime and require no `ffmpeg` or `ffprobe` executable; the WebView owns the replicated OSC, sidebars, OSD, panels, and preferences surfaces.
- The native macOS IINA menu is wired to the focused player session, including dynamic playlist, chapter, video/audio/subtitle/second-subtitle track entries, check states, and playback/sidebar/volume/delay titles. An AppKit bridge restores IINA's Option-only alternate rows for Open/Open URL, frame stepping, fine speed, fine volume, and fine audio/subtitle delay actions after every Tauri menu rebuild.
- Main, secondary, and Mini Player windows now own native AppKit Touch Bars matching IINA 1.3.5's customization identifier, default and allowed item sets, session-local callbacks, exact-percent seek slider, touch-only thumbnail knob/strip, arrow-button modes, time precision, persisted total/remaining toggle, PIP, playlist, volume, and fullscreen escape behavior. The reference hidden Window > Custom Touch Bar... item targets the system customization palette without enabling AppKit's automatic menu insertion.
- Playback A-B Loop now follows mpv's three-state `ab-loop` cycle, polls `ab-loop-a`/`ab-loop-b`/`ab-loop-count`, and exposes draggable A/B timeline knobs plus checked native-menu state.
- Audio Device is rebuilt from the real structured `audio-device-list` mpv Node array, checks the active `audio-device`, and writes the exact selected device name. The packaged-libmpv CoreAudio enumeration gate passes; audible playback and physical device switching remain unlocked hardware acceptance.
- Video Filters and Audio Filters now own two independently reusable `480x382` windows that may coexist, follow application `lastActive`, and retain Add/Save/Edit sheets inside the owning surface. The `440x302` Add sheet reproduces IINA's 11 video presets and 2 audio presets with the reference controls and exact crop/expand/unsharp/lavfi/custom transformations. Saved definitions keep IINA's dictionary format, shortcut fields, checked native submenus, structural comparison, and real mpv Node-list removal.
- The macOS packager implements and has completed the release gates for the final `.app` and UDZO DMG: deep strict signing, bundled-library and localization inspection, `hdiutil verify`/`imageinfo`, and the Finder `500x350` layout. The final bundle is `io.iima.player`, version `1.3.5`, build `141`, with 71 dylibs, 57 localizations, universal libmpv/Safari extension/Sparkle 2.9.4, and arm64 app/CLI executables.
- IINA's macOS document type declarations, plugin UTIs, `iina://` scheme registration, Service entry, and document icons are represented in the bundle contract. The Service is executable rather than declaration-only: `droppedText:userData:error:` marks an external open before validation, reproduces Foundation `openURLString` normalization, and targets the `PlayerCore.active` equivalent represented by `NSApp.mainWindow`; Initial/utility/missing main-window state falls back to the first (`main`) player. It deliberately bypasses both `lastActive` and `alwaysOpenInNewWindow`.
- macOS file-open and `iina://open` / `iina://weblink` events route through a Tauri `RunEvent::Opened` bridge into player state. A normal launch keeps the configured main window hidden for IINA's 100ms open-event grace period, then follows `actionAfterLaunch` (welcome window, Open panel, or no window); an external open suppresses that action, and Dock reopen applies the same preference. URL-scheme enqueue now follows IINA's split targeting exactly: media appends to a nonempty `lastActive` playlist while presentation and `mpv_*` options continue on the separately selected current/new player.
- The macOS Dock context menu now matches IINA 1.3.5 with exactly one localized `Open...` item. It enters the same File > Open target resolution and native media-panel route; a removable runtime subclass supplies only `applicationDockMenu:` without replacing Tauri's delegate object, and shutdown restores its original class and prior Services provider.
- OSC thumbnail preview now follows IINA 1.3.5's FFmpeg-backed behavior through an isolated client loaded from the same packaged `libmpv` dylib as playback: 101 absolute-exact sampled JPEG frames, cancellable background generation, 200ms/10-frame progress batches, an MD5-keyed native-endian v2 cache with source size/time invalidation and 50% oldest-cache eviction, local-volume policy, and the exact 120px preceding-frame hover geometry. Tauri's scoped asset protocol exposes generated previews without broad filesystem access.
- Open File and Add to Playlist now use an AppKit `NSOpenPanel` bridge with IINA's mixed file/directory, multi-selection, alias-resolution, and no-directory-creation settings, then route selected paths through the same backend player state as launch-open events.
- `alwaysOpenInNewWindow` now defaults to IINA's `true`. Normal and Option-alternative Open/Open URL actions invert that preference only while media is playing, reuse an idle player before allocating another session, and rebuild their native menu titles to match the active behavior. Finder batches, URL-scheme opens, menu dialogs, recent items, and native drops now share session-aware open routing instead of falling back to the main player.
- Runtime recent-document and last-playback state now feeds the initial window Resume and Recent Files areas. An AppKit bridge makes `NSDocumentController.shared.recentDocumentURLs` the macOS source of truth, distinguishes immediate Open-panel recording from `trackAllFilesInRecentOpenMenu` file-loaded recording, mirrors record/clear changes into every player and File > Open Recent, and reproduces IINA's macOS 14+ empty-list recovery with URL bookmarks plus fallback URLs. Every libmpv client receives the real app-data `watch_later` directory plus IINA's resume options before initialization; stop, Mini Player close, player close, and app exit queue `write-watch-later-config` before unload and persist the last file/position. Startup exposes Resume only when IINA's `recordRecentFiles` and `resumeLastPosition` preconditions hold and the saved local file still exists.
- Open URL now owns one reusable independent `576x270` AppKit-backed window with full-size content, hidden title/traffic lights, background dragging, close-to-hide reuse, reset-on-show fields, URL validation, inferred `http://`, and HTTP authentication. Reusable context retains alternate/enqueue intent, but Open resolves the latest `lastActive` player at submission time. A Security.framework bridge reads/upserts `IINA Saved HTTP Password` Internet Password items by normalized host and optional port; URL edits safely refill credentials, and the reference reset/off plus opt-in save behavior is preserved.
- File drag-and-drop now routes through Tauri's native drag/drop event bridge; URL and text drops are handled in the frontend and reuse the same open-media path.
- IINA's `MPVController.observeProperties` contract is mirrored as a Rust/Tauri command, player state now carries an mpv-style property snapshot for path, media-title, duration, time-pos, pause, volume, speed, mute, tracks, playlist, chapters, and idle-active, and the Rust backend now exposes an IINA-ordered mpv playback-session plan for create/options/wakeup/observe/initialize/libmpv-render setup.
- A dynamic `libmpv` client smoke command now loads the dylib when present, resolves the IINA-required symbols, creates an mpv client, applies safe startup options, requests warn logs, observes IINA's 34 properties, initializes mpv, and terminates/destroys the client; on machines without a dylib it returns a structured unavailable report instead of pretending playback is available.
- The macOS player now hosts its native `NSOpenGLView` in a full-size opaque borderless child `NSWindow` ordered below the transparent Tauri parent window, avoiding AppKit sibling-view occlusion while preserving the WebView as overlay chrome. The view binds to the executor's one libmpv client through `mpv_render_context`; redraws are gated by `CVDisplayLink`, follow parent move/resize/fullscreen transitions, restore after six idle seconds, synchronize `override-display-fps`, resolve the active display's ColorSync ICC profile for SDR, and apply IINA-style HDR/EDR plus tone-mapping settings from runtime video metadata and preferences. Initial video-sized window changes are followed by an AppKit-main-queue center operation, matching IINA's centered `640x400` initial window and centered aspect-preserving playback resize instead of retaining the initial top-left corner.
- Native parent/child geometry notifications are coalesced behind a short main-queue quiet period, and output color-space/EDR assignment now follows IINA 1.3.5 by remaining on the OpenGL backing layer instead of mutating `NSWindow.colorSpace`. The compiled native exception harness and HDR source contract pass. After this change, packaged stdin playback passed one 5-second and three 10-second strict soaks; every run reached `loadfile -`, libmpv video output, and `playback-restart` without `CGSSetSurfaceColorSpace`, an uncaught exception, abort, or missing render-context error.
- Native Picture in Picture moves that same render view into the system PIP controller and now supplies IINA-equivalent `replacementWindow`/`replacementRect` state before closure, then defers controller teardown until AppKit has restored the view. MainWindow uses the exact `87x68` reference replacement artwork with its localized label 8 points below, theme-aware material/template tint, and preemptive user/native-close hiding before the render view returns. Native contracts cover PIP ownership/restoration; final packaged GUI playback additionally verified fullscreen enter/exit and post-resize continued rendering without process termination.
- Music Mode now uses a dedicated IINA-sized Mini Player: a `300x72` control bar, square default album art, aspect-correct native video, optional `300px` compact playlist, exact reference control positions, 200ms info/control transition, `183x29` volume popover, chapter-aware title/album/artist metadata, persisted album-art/playlist visibility, and local-audio auto-switch semantics with manual overrides. AppKit removes the normal title controls and only floats the window when `alwaysFloatOnTop` is enabled; Back restores the player while Close stops playback and closes the retained Main surface without inventing a visible Welcome transition. Its native layout observer also reproduces `MiniPlayerWindowController` live resize: the playlist is revealed while expanding, the strict `normal + 200pt` end threshold controls fold/unfold, collapse and playlist toggles animate around a fixed top edge, and video/aspect changes resize immediately from the bottom edge.
- Player open/enqueue/command paths now record sequenced equivalent IINA/mpv operations (`loadfile replace/append`, `seek absolute+exact`, `set pause/volume/speed/mute`, `frame-step`, `playlist-next/prev`, and track property changes), giving the dynamic `libmpv` executor a tested command contract instead of a UI-only state model.
- The bundled `iina-cli` now follows IINA's native process boundary instead of translating command-line launches into deep links: it resolves the sibling main executable; makes existing media paths absolute while lexically removing `.`/`..` without resolving media symlinks; preserves unknown dash-prefixed arguments for the main executable; normalizes prefixed and raw mpv options; supports separate windows, Music Mode, PIP, and keep-running; auto-detects or explicitly selects stdin; and directly inherits the media pipe while the main process opens libmpv target `-`. The main parser ignores unknown long options and applies IINA's `dropNextArg` rule to unknown single-dash option/value pairs; an all-invalid non-separate target list still allocates one option-configured idle player and performs an empty open, while separate-window mode allocates none. Command-line mpv properties are applied before `loadfile`; the reference-special `--mpv-shuffle=yes` is consumed as a one-shot `playlist-shuffle` then `playlist-play-index 0` only after the whole playlist has been appended; and stdin is excluded from recent documents.
- Every configured new-media path explicitly synchronizes mpv's persistent global `pause` property with IINA's `pauseWhenOpen` preference after `loadfile`; opening another file after EOF therefore starts or pauses according to the preference instead of inheriting the previous file's stale pause state.
- A persistent mpv executor state is now exposed through Tauri commands, accepts operations from frontend open/player commands plus Launch Services, drop, and menu paths, avoids duplicate submissions with the player log sequence cursor, initializes a long-lived `libmpv` client when a dylib is present, drains queued `mpv_command`/typed property operations into the native client, and polls `mpv_wait_event(..., 0)` for event/property-change summaries; without a dylib it reports explicit unavailable-runtime evidence and keeps operations pending.
- Every long-lived client now registers a real `mpv_set_wakeup_callback`. A shared race-safe condition signal wakes the backend synchronizer immediately, which drains events for the main and every independent player session; a 250ms timeout remains as the authoritative property-snapshot fallback.
- Drained mpv property-change events for the IINA-observed core playback properties now reduce back into `PlayerState` on executor sync/status calls, covering pause, volume, speed, mute, media title, primary and second-subtitle track ids, chapter, and idle-active snapshots.
- Executor sync/status calls now also poll authoritative `libmpv` properties for path, media-title, duration, time-pos, percent-pos, pause, volume, speed, mute, chapter counts, playlist position/count, track count, selected track ids, and idle-active; those snapshots reduce back into `PlayerState`, including progress-bar time, duration, current playlist item, and Resume position.
- mpv `start-file`, `file-loaded`, `playback-restart`, `seek`, `end-file`, and `idle` events now decode into structured Tauri event data where applicable, including `mpv_event_start_file.playlist_entry_id` and `mpv_event_end_file` reason/error/playlist insertion fields. The loading reducer follows IINA's ordering: a non-stop `end-file` received while loading is held as a pending structured error and only closes the player state after `idle-active=true`; the frontend then presents IINA's exact generic `Cannot open file or stream!` alert once.
- Player-window titles now follow `MainWindowController.updateTitle`: local absolute paths and decoded `file://` paths set AppKit `representedURL` and the represented filename/proxy icon; network media clears any stale represented URL and uses the current media title; stop, EOF, retained Close, and Initial state clear the proxy and restore `IINA`. Legacy/untitled fullscreen uses the safe last-path-component fallback and immediately re-synchronizes when the title style changes. The route stays on the owning decorated player surface and excludes `disableUI` plugin windows.
- The executor now polls mpv `track-list/N/*` subproperties into structured track payloads and reduces them into IINA-style video/audio/subtitle groups with selected ids, language, codec, external/default/forced flags, decoder description, dimensions, fps, audio channel, and samplerate metadata.
- The video/audio/subtitle sidebar tabs now render the richer runtime track metadata as structured detail lines and status badges, and clicking a track sends an exact `select-track` command that writes `vid`/`aid`/`sid` instead of only cycling to the next track.
- Video/Audio/Subtitles sidebar tabs now also expose IINA-style runtime Quick Settings for hardware decoding mode, an EDR-aware HDR switch that resets from `enableHdrSupport` only for each newly opened media session, aspect-ratio presets and validated custom `width:height` ratio, crop presets generated from the selected video's dimensions, deinterlace, 0/90/180/270 degree rotation, horizontal mirror/vertical flip, brightness/contrast/saturation/gamma/hue, the 10-band audio equalizer, audio delay, subtitle delay, subtitle scale, subtitle position, and the reference subtitle text-color/font-size/border/background/font options. The audio and subtitle panels use native filtered file dialogs for IINA's external-track loading paths, mapping to `audio-add`, `sub-add`, and same-path `sub-reload`; property controls write the corresponding libmpv values and receive runtime changes back into the control state.
- The executor now also polls IINA's `playlist/N/filename`, `playlist/N/current`, `playlist/N/playing`, `playlist/N/title`, and `playlist/N/id` contract into structured playlist items, preserving mpv ids and playing state while reducing the runtime playlist back into `PlayerState`.
- Playlist sidebar rows now follow IINA's double-click playback behavior: double-clicking a playlist row sends `select-playlist-item` and records a `playlist-pos` property write, while double-clicking a chapter row seeks to that chapter time.
- Playlist sidebar rows now expose IINA-style row context actions for Remove from Playlist and Clear Playlist, mapped to mpv `playlist-remove` and `playlist-clear` operations while keeping the current item selected when clearing.
- Playlist rows now enrich from a per-player serialized cache with duration/title/artist obtained through the isolated packaged-libmpv media helper and mpv watch-later progress, render the exact preference-gated metadata/progress/total-and-selected-duration contract, accept native file and external URL drops at the pointed insertion row, and support AppKit-backed Copy/Cut/Paste. Mixed selections partition local and network actions; multi-file Move to Trash removes only the rows whose filesystem operations succeeded.
- A backend-owned wakeup-driven mpv synchronizer keeps player state current even when no web window is visibly polling, and falls back to authoritative property polling every 250ms. Active player windows use a visibility-aware 100ms authoritative snapshot poll with unchanged snapshots fingerprinted to avoid resetting overlays or repainting unnecessarily; only 10ms/1ms OSC time precision enables a lightweight 40ms local time-display interpolation ticker, leaving backend traffic at 100ms.
- A macOS `libmpv` dynamic-loader status command checks the packaged dylib first, retains repository/Homebrew candidates for development fallback, and resolves the client/render symbols IINA uses before the native surface or isolated media helper creates a client.
- The macOS packaging path copies IINA's 71 mpv/FFmpeg dylibs under `Contents/Frameworks`, patches the single local `libmpv`/`libplacebo` ABI suffix mismatch in the app copy, adds app/libmpv rpaths, ad-hoc signs the app, and verifies that bundled `libmpv.2.dylib` exposes the IINA client/render symbols. All of these checks, including deep strict signing, pass on the final package.
- The IINA application menu and General preferences expose the Sparkle update flow from 1.3.5: Check for Updates, automatic checks, the exact Hourly/Daily/Weekly/Monthly intervals, and beta-feed selection. A main-thread Objective-C bridge dynamically loads the packaged universal Sparkle 2.9.4 framework, clears legacy feed overrides, and reads stable/beta HTTPS feeds from the signed bundle. Default packaging preserves only IINA's exact reference updater plist keys and byte-identical DSA key. Opt-in production mode requires Tauri-owned feeds plus a canonical 32-byte Ed25519 public key, removes stale IINA DSA/private plist state even when reusing a bundle, rejects IINA artifact reuse, and fails closed rather than falling back to an IINA feed. The bundled Sparkle tool generates signed appcasts with private-key material supplied through stdin and removed from argv, logs, and the child environment. `npm run appcast:test` compiles an independent fake-Sparkle Objective-C harness against the production bridge and proves that initialization, settings, manual-check, and status exceptions are returned through the C/Rust boundary while poisoned runtime state is discarded and remains retryable. Universal Sparkle 2.9.4, nested components, and deep signatures pass in the final bundle.
- IINA 1.3.5's 56 non-Base locale tables are generated deterministically from the reference `.lproj` resources into 33,847 translated source-fallback entries plus 65,181 exact `table:key` context entries. Exact context lookup resolves all 730 per-locale English-source collisions while preserving the existing source fallback for missing contexts. The frontend selects exact, language-family, and Chinese/Serbian script fallbacks from the WebView locale, applies `lang`/RTL `dir`, translates static and dynamically created main/history/preferences/filter/OSD/Quick Settings surfaces, and formats IINA's `%@`/integer/floating-point/escaped-percent templates for OSD, utility results, and playlist duration summaries. A compact Rust catalog uses the same fallback rules and context identifiers with AppKit's preferred-language order to localize every app-owned native menu title, dynamic speed/volume/delay label, Playback History and Save Downloaded Subtitle title, and native font picker. The sole reference `Translators.stringsdict` is hash-pinned and verified to contain only empty plural-category placeholders, so IINA 1.3.5 supplies no plural text to reproduce there. User filenames, media metadata, saved-filter names, plugin-defined text, and other externally owned labels remain literal. The final package contains all 57 reference `InfoPlist.strings` resources byte-for-byte and passes release embedding/signing checks; exhaustive per-locale visual overflow/RTL review remains an external presentation boundary.
- About IINA now uses the reference custom window instead of Tauri's stock About panel: one reusable `640x400` full-size-titlebar window preserves the `220+420` layout, 80-point app icon, `1.3.5 Build 141`, concise mpv/FFmpeg versions read by a short-lived no-output client from the same libmpv dylib candidate set as playback, and localized License/Contributors/Credits tabs. It never substitutes unrelated command-line tools when the bundled runtime is unavailable. Generation retains all 29 localized `Contribution.rtf` documents and the complete `Credits.rtf` as both exact plain-text fallback and Cocoa-formatted HTML, with source hashes. Contributors load the paginated GitHub API into the reference 32-point circular grid; failed/offline pages or avatars remain empty rather than inventing data, and every document/footer link crosses an exact native opener allowlist. Closing hides the non-released window, so the application-menu item reuses and focuses the same instance.
- Playback > Take Screenshot now runs IINA's asynchronous `mpv screenshot subtitles|video` command on the invoking player session's persistent libmpv client, matches command replies through userdata `1000000`, applies mpv's screenshot directory/format/template properties, and reads the subtitle/save/preview/clipboard preferences. A failed command presents IINA's exact `Cannot take a screenshot!` alert.
- Screenshot success now shows an IINA-style preview OSD capped at 300x200 with DELETE, EDIT, and REVEAL file actions when the screenshot is saved to disk.
- Normal, detail, progress, and screenshot OSD paths now follow IINA 1.3.5 message identity and replacement behavior, exact compact/detail/progress geometry, one-second visibility plus a half-second fade, and the five-second screenshot-preview lifetime. Pause, volume, seek, speed, and screenshot paths share the same timed overlay owner instead of independent DOM notices.
- `screenshotCopyToClipboard` writes the mpv-created image to the macOS pasteboard; non-saved captures use the app's `screenshot_cache` directory and are removed after clipboard/preview ownership is established.
- Playback > Show Screenshot Folder resolves the same screenshot folder preference and asks macOS Finder to open it.
- Preferences... opens an IINA-style source-list window with the 1.3.5 pane set, searchable controls, and screenshot destination/format/template options. Its completion index now follows IINA's localized NIB scan and suffix-Trie matching, preserves section/control order, supports bounded arrow/Enter/Escape navigation, exact no-result/clear/focus behavior, dependency fallback, and delayed scroll/highlight navigation. Audio/subtitle preferred-language fields use AppKit-style rounded editable tokens with the complete reference ISO-639 catalog, name-prefix completion, duplicate exclusion, CSV paste, and ordered persistence. `preferences.json` remains authoritative and is mirrored to a flat binary `com.colliderli.iina.plist` beside it; loading prefers JSON, falls back to that local mirror, and imports the original `~/Library/Preferences/com.colliderli.iina.plist` read-only only when neither local store exists. The compatibility conversion preserves plist Data, Date, UID, non-finite real, array, and dictionary values; JSON `null` stays in JSON because UserDefaults has no null representation. The UI pane now follows all six reference sections and defaults, with live native/WebView appearance, physical and automatic video-size/geometry behavior, playback float-on-top, all three OSC layouts and toolbar customization, OSD/thumbnail/fullscreen filename-time-battery information, and reversible PIP window behavior.
- The Utilities pane now reproduces IINA 1.3.5's four reference sections and copy: a `448x192` all-selected Video/Audio/Playlist sheet registers bundled extensions through macOS LaunchServices, suppressed alerts reset persistently, saved playback progress recreates `watch_later`, playback history clears `history.plist`, Open Recent, and every player session's Resume state, thumbnail cache size/clear remains integrated, and the exact Chrome/Firefox extension links use an allowlisted native opener.
- Preferences uses one reusable `820x480` full-size-content window with an `820x320` minimum, close-to-hide reuse, and conditional AppKit frame restore/autosave under `IINAPreferenceWindow`. Shared preference writes broadcast monotonic full snapshots application-wide, and a revisioned post-listener handshake closes startup races. The utility WebView creates no plugin realms: manager mutations refresh only real player hosts, while reusable context restores an optional installed-plugin selection on Permissions. Finder/cold-start packages remain queued and open Preferences without showing or focusing Main.
- File > Playback History now opens one reusable retained `600x400` window with a `400x200` minimum. Close hides the non-released window and AppKit restores/saves its frame under `PlaybackHistoryWindow`; Date/Folder grouping, search, expandable groups, progress/played-at columns, missing-file state, selection, playback, Finder reveal, and delete confirmation remain. `file-loaded` records a stable mpv-path-MD5 entry in an IINA-compatible binary `NSKeyedArchiver` `history.plist`, and progress derives from the matching real mpv `watch_later` file.
- Log Viewer now owns one reusable retained `600x335` window; Close hides the existing instance and AppKit restores/saves its frame under `IINALogViewer`.
- OpenSubtitles account preferences now reproduce IINA's `Not logged in` / `Logged in as %@` state, localized Login/Logout controls, and username/password sheet. Credentials are verified against the OpenSubtitles login endpoint before the username is persisted and the password is upserted as a Generic Password under the exact `IINA OpenSubtitles Account` Keychain service. A redacted 23-hour app-wide session follows the provider-returned API host; later searches reuse it, expired or server-rejected tokens are abandoned, and a restart restores the configured password from Keychain before authenticated search/download. Login, search, download-link, and temporary-link content requests share IINA's response-header rate limiter; successful login resets the endpoint-scoped limit, and the content request carries the same authorization headers. Logout clears only `openSubUsername`, preserving IINA's stored-password behavior.
- Plugin preferences now reproduce the reference `480x545` master-detail pane: a `160x400` single-selection list with 36-point enable rows drives a `312x400` detail pane whose `195x21` Permissions/About/Preferences segmented control embeds permission summaries plus the existing help and preference HTML bindings; changing selection resets to Permissions and unloads stale iframe/page state. Single-row drag and drop persists IINA's plugin order, while Show in Finder accepts only an installed identifier and resolves the package root natively instead of accepting a frontend path. The `480x297` GitHub sheet includes IINA's three default repositories, installed-state column, selection-to-source behavior, and busy spinner. It installs hardened local `.iinaplgz` packages or GitHub `owner/repository` sources, discovers `.iinaplugin-dev` source links without deleting the source when unlinked, prefers a release package before falling back to `main.zip`, preserves GitHub `ghRepo`/`ghVersion` identity, and offers per-plugin GitHub check/update actions. Duplicate identifiers from the local picker, Finder/file drops, or GitHub share one staged reinstall confirmation: Cancel leaves the old plugin and state byte-for-byte unchanged, while Confirm uses a SHA-256-verified, crash-recoverable backup/rename transaction, preserves the latest enabled/order state, and safely unlinks a development link without deleting its source. Standard preference inputs persist through the plugin preference store; `iina.http.download` honors IINA's domain/network permissions and safely writes to private plugin paths. The runtime also exposes IINA-style prioritized keyboard/mouse listeners, listener-ID event subscriptions, and reference-synchronous Core/mpv/file/binary-handle/Utils/standalone-state/WebSocket-create-start calls through an owner-bound custom-protocol grant that is never exposed to plugin realms; reference Promise methods remain Promises. File text decoding covers the complete case-sensitive IINA encoding catalog, atomic private writes do not invent nested parents, directory listing preserves IINA's direct-child/symlink and `isDir` shape, and write-only handle reads return `null`. Native plugin events use an independent per-player cursor and lossless handoff, so repeated mpv events and same-value property notifications remain observable; the full 17-event IINA-owned window/file/PIP/Music Mode/thumbnail/overlay contract and dynamic changed-property observation are covered by executable tests.
- The native Plugin menu projects the active `PlayerCore` equivalent in persisted plugin order: `Manage Plugins…`, global items before the owning entry items, disabled plugin headers, the first five root items plus `More…`, checked/enabled submenu state, shortcut-conflict suppression and warning, `Developer Tool`, and `Reload all plugins`. Menu clicks return to the exact owner/role realm. Each Developer Tool item reuses a close-to-hide `500x400` console bound to one exact JSContext identity, forwards typed results, exceptions, and role-scoped logs, and provides the reference split history/editor interaction. The exact Reload target is fixed by `MainMenuActions.reloadAllPlugins`: rebuild every enabled entry instance only in the active player while preserving the primary global realm/controller/state/menu and global Developer Tool. Entry-owned standalone pages, WebSockets, synchronization grants, file/binary handles, callbacks, hooks, and menu state are revoked without touching their global-owned counterparts; an opened old entry Developer Tool retains only its retired realm's evaluation/log context and cannot operate the replacement runtime. The complete Node/Rust reload and context-isolation contracts pass.
- Chrome Manifest V3 and Firefox Manifest V2 Open In IINA artifacts are reproduced under `browser/`, including toolbar/popup/options plus page/link/video/audio context menus, and route through registered `iina://open`; the separate-window popup action sends `new_window=1`. IINA 1.3.5's Safari `SFSafariExtensionHandler` App Extension is also reproduced with its exact `iina://weblink` page/link behavior, compiled offline as a universal `OpenInIINA.appex`, embedded under `Contents/PlugIns`, and sandbox-entitlement ad-hoc signed before the outer app is sealed.
- Key Bindings now exposes IINA's four read-only built-in configurations (IINA, mpv, VLC, and Movist) plus safe user-profile New, Duplicate, Import, edit, Delete, Reveal, and Export flows. The exact IINA default parses to `83 Shortcuts | 0 Raw`; all 19 IINA private commands and generic mpv commands share executable runtime paths, `{default}` sections are active while named sections remain inactive, duplicate keys follow mpv's last-wins rule, and cleanup is labeled Keep Last. The selected profile is materialized as a real `input-conf` path before mpv initialization; legacy empty-array defaults migrate to `null`, while a version-2 empty array remains an intentionally empty profile.
- One shared IINA-command router covers the complete 19-command private surface. Native actions now include Half/Normal/Double/Fit/Bigger/Smaller window sizing, a localized Jump To prompt with absolute-exact seek, ordered UTF-8 M3U8 playlist saving with trailing newlines and atomic replacement, and current-local-file Trash/permanent deletion in IINA's remove-playlist-item, synchronize-mpv, then filesystem order. Menu actions fall back to the last active player when focus is on a non-player window. Visible native menu equivalents are rebuilt from the active last-key-wins profile through a Cocoa logical-key bridge, so Unicode/localized saved-filter shortcuts and IINA's function/navigation/keypad names avoid Tauri's physical accelerator parser; matched numeric seek/speed/volume/delay values update both titles and the command payload. Additional keys mapped to one action remain executable through IINA's hidden File-menu duplicates. Edit commands use native first-responder selectors, Subtitle Encoding mirrors all 44 reference rows and `sub-codepage`, and online-subtitle source selection covers built-ins plus enabled plugin providers without mutating the default preference.
- Player-surface mouse behavior uses IINA's per-control action choices. A single click waits 500ms and is cancelled by a double click; single/double/right/middle actions share fullscreen, pause, hide-OSC, and PIP routes as allowed by each reference control. Hide OSC is one-way until pointer movement restores the OSC and cursor, plugin mouse callbacks retain high/default/low priority, and no video context menu is invented because IINA 1.3.5 has none. Vertical/horizontal wheel volume/seek routing remains preference-backed.
- The OSC reproduces IINA 1.3.5's reference geometry and artwork, authoritative play/pause, mute, volume, seek, fullscreen, A-B loop, playlist, and settings state, plus state-dependent control enablement. Its configurable left/right buttons implement the exact 11-value speed ladder and direction-change jumps, previous/next playlist mode, or 10-second rewind/fast-forward mode; the speed labels appear on the matching side and reset through the play button as in IINA.
- The right OSC time toggles persisted total/remaining display on click and exposes IINA's `1s`, `100ms`, `10ms`, and `1ms` precision menu on secondary click. Negative remaining time and subsecond labels use content-sized outer columns so they cannot escape the reference OSC bounds.
- Window > Inspector now owns a reusable `350x430` AppKit utility/HUD window with exact General/Tracks/File/Status localization, one-second cross-session `lastActive` refresh, live libmpv/player/track/renderer/file data, and bounded editable `watchProperties` persistence without exposing generic mpv IPC.

The repository-owned and locally reproducible scope is now 100% closed: the current source, native bridges, final app/DMG, packaged stdin path, real WebKit plugin boundary, real libmpv/media-helper path, About runtime versions, cold launch, and long-running GUI playback all pass their final gates. This local 100% does not claim environment-owned acceptance. Physical Touch Bar/media-key/power/input/trackpad/HDR/multi-display behavior; real accounts, Keychain providers, and third-party plugin samples; Finder/Services/pasteboard/Trash/drop interaction; installed browser extensions and stores; Apple Developer signing/notarization; a project-owned HTTPS appcast/release channel; and exhaustive per-locale visual review remain external acceptance boundaries, not unfinished repository implementation. The reproduced upstream IINA appcasts publish native IINA builds and therefore cannot serve as the Tauri release channel.

## Commands

```sh
npm run build
node --check scripts/package-macos.mjs
cargo check --offline --manifest-path src-tauri/Cargo.toml
cargo test --offline --manifest-path src-tauri/Cargo.toml
cargo build --release --offline --manifest-path src-tauri/Cargo.toml
npm run gui:stdin-playback
npm run package:mac
```

The current environment does not have a local or global Tauri CLI. `npm run package:mac` therefore uses `scripts/package-macos.mjs` to rebuild the frontend and release binaries with Tauri's `custom-protocol` and macOS-only `macos-cli` features, refresh the Tauri `.app`, copy IINA's 71 dylibs and universal Sparkle 2.9.4, enforce the updater plist contract, patch the checked dependency's libplacebo ABI suffix, add the required rpaths, ad-hoc sign and deep-verify the bundle, smoke-load libmpv/Sparkle, and generate a UDZO DMG. The script follows Finder's actual custom volume name, applies the `500x350` icon layout, and passes all final package gates.

Final artifact paths:

- `src-tauri/target/release/bundle/macos/IINA.app`
- `src-tauri/target/release/bundle/dmg/IINA_0.9.0_aarch64_bundled_libmpv.dmg`

Current local `0.9.0` package evidence:

- app identity `0.9.0` / build `90`, arm64 main executable, passing deep strict ad-hoc signing;
- DMG: `57,699,103` bytes, SHA-256 `1ac95f4419964c26d641b8397099728fceae74b66cad7783a15ceee7fd034930`, passing `hdiutil verify`;
- main executable SHA-256 `780ebded9159eb7c33999ee43a4480df3e3f5c24e4e0d3e0bb10d6aa4e5f1eb8`;
- CLI SHA-256 `88b4671115de4d70aec2cbe8980987ba510f303902735339d54af3bf0076260b`.

## GitHub Actions packages

`.github/workflows/build-packages.yml` builds two downloadable workflow artifacts on pushes to
`main`, pull requests, and manual runs:

- `IINA-windows-x64`: an unsigned NSIS x64 installer built on `windows-latest`;
- `IINA-macos-arm64`: an ad-hoc-signed arm64 `.app` archive and verified DMG built on the Apple
  Silicon `macos-15` runner.

The 2.6 GiB local `参考/` workspace is intentionally ignored. The macOS job recreates only its
required inputs from the pinned IINA commit and Sparkle release by running
`scripts/prepare-macos-ci.sh`. Build artifacts and local caches are also excluded from Git.

The Windows package is currently an unsigned portability build. The replicated native video and
dynamic libmpv integration remain macOS-specific, so the Windows artifact validates that the Tauri
desktop shell packages successfully; it does not claim macOS playback-feature parity.

Final artifact evidence:

- `IINA.app`: `141152 KiB`; arm64 `IINA.app/Contents/MacOS/iima` is `18,510,832` bytes, SHA-256 `0d6a4dd8176fc2d69c01525481a955f0f172a2a8ca712ba867bf0a034cbfc322`.
- `iina-cli`: arm64, `516,976` bytes, SHA-256 `3ecd91586acc25a6282de3ed6580ef07e34140854a3e1aaee81c7c23028c8a84`.
- DMG: `57,710,770` bytes, SHA-256 `a456f8bb561e2ab39d6f8f3c1cc83f9ae39415897b72fedff9c8ac35f189e627`; UDZO GUID/HFS+, compressed bytes `57,693,499`, CRC32 `C017ED72`, and `hdiutil verify` reports `VALID`.

Final verification evidence: `npm run verify:offline` passes the current source with 520 Rust library tests total (516 passed, 4 explicitly ignored external hardware/fixture gates), 6 CLI tests, the frontend/native/plugin suites, and the all-target check. The native-video registry main-thread C ABI harness passes. Cold launch passes 3/3 in `1366/962/1979 ms`; the final strict stdin 10-second gate and real packaged WebKit probe pass; the real selector passes 20/20 with a 30-second MP4 helper result (`796474` bitrate, 1 track, 0 chapters, JPEG `160x90`); About reports mpv `0.38.0` and FFmpeg `7.0.1`. A five-minute packaged GUI run played beyond `2:50`, held position while paused, advanced after resume, entered/exited fullscreen, continued after using the window zoom/resize control, and continued rendering without a crash.
